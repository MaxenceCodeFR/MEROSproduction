{% extends "base.html.twig" %}

{% block title %}Candidature
{% endblock %}

{% block body %}
	{% include "commons/_ceo_navbar.html.twig" %}

	<div class="container mt-4">
		<div class="row">
			<div class="col-md-6">
				<h2>{{ company.email }}</h2>
				<p>{{ company.motif.motifCompany }}</p>
				<p>{{ company.firstname }}</p>
				<p>{{ company.firstname }}</p>
				<p>{{ company.text }}</p>
			</div>
			<a href="mailto:{{ company.email }}" target="_blank" class="btn btn-primary">Répondre</a>
		</div>
	</div>
{% endblock %}
